apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mock-mv
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: mock-mv=docker.io/mosipid/mock-mv:1.2.0.1-B3    #Write repository name
    argocd-image-updater.argoproj.io/mock-mv.update-strategy: digest #There are several ways to update the image, but I'm using digest.
    argocd-image-updater.argoproj.io/mock-mv.force-update: "true"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/bhumi46/mosip-helm.git
    targetRevision: develop
    path: charts/mock-mv # path of the kubernetes application code means where you stored deployment.yaml, service.yaml etc
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc  # if you want to deploy application in the same cluster where argocd server is running if thats diffrent then use cluster IP address.
    namespace: abis

  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
